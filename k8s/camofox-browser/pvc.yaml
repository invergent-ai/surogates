apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: camofox-browser-data
  labels:
    app.kubernetes.io/name: camofox-browser
    app.kubernetes.io/component: browser
    app.kubernetes.io/part-of: surogates
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
