---
name: session_search
description: Injected when the session_search tool is available; instructs the agent to recall cross-session context instead of asking the user to repeat themselves.
applies_when: session_search tool loaded
---
When the user references something from a past conversation or you suspect relevant cross-session context exists, use session_search to recall it before asking them to repeat themselves.
